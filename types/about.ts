export interface Skill {
  category: string
  color: string
  items: string[]
}

export interface Experience {
  title: string
  period: string
  color: string
  description: string
}

export interface Value {
  title: string
  description: string
  icon: string
  gradient: string
} 